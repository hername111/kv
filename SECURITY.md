# 安全说明

## 支持范围

本项目是课程教学数据库，不提供生产环境安全承诺。默认服务绑定到 `127.0.0.1`，MySQL 兼容
接口和演示 HTTP API 均未实现身份认证、传输加密、租户隔离或权限控制。

不要把服务绑定到公网地址，不要存储真实业务数据、密码、令牌或个人敏感信息。

## 问题报告

发现越界访问、拒绝服务、数据损坏或未授权访问问题时，请通过仓库的私有安全报告渠道联系
维护者；仓库未托管时，应直接联系课程项目组。报告应包含复现步骤、影响范围、运行环境和最小
测试样例，不要在公开 issue 中附带敏感数据。

## 已知边界

- MySQL 握手仅用于协议兼容演示，不验证账号密码。
- 演示 HTTP API 面向单用户本地环境，会话在服务进程内共享。
- 数据文件没有加密，也没有 WAL 提供崩溃原子恢复。
- SQL 子集不构成完整的 MySQL 安全或权限模型。
